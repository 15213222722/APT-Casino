# 欧洲轮盘游戏动画集成指南

## 概述
本文档提供了将动画效果集成到现有欧洲轮盘游戏的详细指南。

## 🎯 新增动画功能

### 1. 筹码投注动画
- **筹码飞行**: 点击投注区域时，筹码会从点击位置飞向目标位置
- **投注脉冲**: 成功投注后，投注区域会有金色脉冲效果
- **粒子爆炸**: 投注成功时在投注位置产生彩色粒子效果

### 2. 游戏结果动画
- **获胜数字高亮**: 中奖数字会有特殊的缩放和发光动画
- **转轮旋转**: 流畅的转轮旋转动画，带有缓动效果
- **胜利庆祝**: 获胜时触发全屏粒子效果和数字跳动

### 3. UI反馈动画
- **错误摇摆**: 余额不足或连接问题时按钮摇摆提示
- **计数动画**: 投注总额更新时的放大缩小效果
- **平滑过渡**: 所有交互都有流畅的过渡效果

## 🔧 集成步骤

### 步骤 1: 导入动画组件
在 `page.jsx` 中添加以下导入：

```javascript
import AnimatedRouletteGame from './AnimatedRouletteGame';
import { enhancedAnimations, animationClasses, injectAnimationStyles } from './enhanced-styles';
import { animationUtils } from './animations';
```

### 步骤 2: 初始化动画样式
在组件挂载时注入动画样式：

```javascript
useEffect(() => {
  injectAnimationStyles();
}, []);
```

### 步骤 3: 包装游戏区域
用 `AnimatedRouletteGame` 组件包装现有的游戏界面：

```javascript
<AnimatedRouletteGame 
  onPlaceBet={handleEnhancedPlaceBet}
  gameState={gameState}
  result={rollResult}
  spinning={wheelSpinning}
>
  {/* 原有的游戏内容 */}
  <YourExistingGameComponents />
</AnimatedRouletteGame>
```

### 步骤 4: 增强投注函数
修改现有的 `placeBet` 函数来支持动画：

```javascript
const handleEnhancedPlaceBet = useCallback((betInfo) => {
  const { type, ind, amount } = betInfo;
  
  // 获取点击位置用于动画
  const event = betInfo.event;
  const rect = event.currentTarget.getBoundingClientRect();
  const position = {
    x: event.clientX - rect.left,
    y: event.clientY - rect.top
  };
  
  // 调用原有的投注逻辑
  placeBet(event, type, ind);
  
  // 触发动画效果
  // (这会自动由 AnimatedRouletteGame 处理)
}, [placeBet]);
```

## 🎨 自定义动画

### 修改动画时长
在 `enhanced-styles.js` 中调整关键帧动画：

```javascript
const chipFlyKeyframe = keyframes`
  0% { /* 起始状态 */ }
  100% { 
    transform: scale(0.6) translateY(-40px);
    opacity: 0;
    /* 修改动画距离和时间 */
  }
`;
```

### 自定义粒子颜色
在 `AnimatedRouletteGame.jsx` 的 `ParticleExplosion` 组件中：

```javascript
color: ['#FFD700', '#FF6B6B', '#4ECDC4', '#45B7D1'][Math.floor(Math.random() * 4)]
// 修改为你的品牌色彩
color: ['#YOUR_COLOR_1', '#YOUR_COLOR_2', '#YOUR_COLOR_3'][Math.floor(Math.random() * 3)]
```

### 调整动画缓动
在 `AnimatedRouletteGame.jsx` 中修改 motion 组件的 transition：

```javascript
transition={{
  type: "spring",
  stiffness: 300,  // 调整弹性
  damping: 20       // 调整阻尼
}}
```

## 🚀 性能优化建议

1. **限制粒子数量**: 在移动设备上减少粒子数量
2. **使用 CSS transforms**: 优先使用 transform 而非改变布局属性
3. **避免内存泄漏**: 及时清理动画定时器
4. **条件渲染**: 只在需要时渲染动画组件

## 🐛 故障排除

### 动画不显示
- 检查 CSS 样式是否正确注入
- 确认浏览器支持 CSS 关键帧动画
- 验证 JavaScript 控制台是否有错误

### 性能问题
- 减少同时播放的动画数量
- 使用 `React.memo` 优化组件重渲染
- 考虑使用 `requestAnimationFrame` 替代 setTimeout

### 移动设备适配
- 检测触摸设备并调整动画强度
- 考虑降低移动设备上的动画帧率

## 📱 响应式设计

动画系统已内置响应式支持：
- 自动检测屏幕尺寸
- 在小屏幕上减少动画复杂度
- 支持横竖屏切换

## 🎮 使用示例

查看 `AnimatedRouletteGame.jsx` 中的示例实现，了解如何：
- 创建粒子爆炸效果
- 实现筹码飞行动画
- 设计获胜数字高亮
- 处理游戏状态转换

---

**注意**: 确保在生产环境中测试所有动画效果，确保兼容性和性能。